name: Build Binaries

on:
  push:
    # tags:
    #   - "v*"
    branches: ["main"]
  workflow_dispatch:

jobs:
  build:
    runs-on: ${{ matrix.os }}

    strategy:
      matrix:
        os: [[ubuntu-latest, windows-latest]]

    steps:
      - name: Checkout Code
        uses: actions/checkout@v2
      - run: git fetch --prune --tags --unshallow

      - name: Setup Python
        uses: actions/setup-python@v1
        with:
          python-version: "3.10"

      - name: Setup Poetry
        uses: Gr1N/setup-poetry@v7

      - name: Install Poetry Packages
        run: poetry install

      - name: Get version
        run: |
          poetry run python ./build_system/update_version.py

      - name: Build Pyinstaller
        run: |
          poetry run python ./build_system/update_version.py
          poetry run pyinstaller --onefile ./src/zeal_cli.py --name zeal-cli

      - name: 'Upload Artifact'
        uses: actions/upload-artifact@v3
        with:
          name: zeal-cli
          path: dist/zeal-cli
          retention-days: 5

      - name: 'Upload Artifact - Windows'
        uses: actions/upload-artifact@v3
        with:
          name: zeal-cli.exe
          path: dist/zeal-cli.exe
          retention-days: 5

      # - name: Upload Executables
      #   uses: softprops/action-gh-release@v0.1.14
      #   with:
      #     files: |
      #       dist/zeal-cli-${{get_version.outputs.version}}
      #       dist/zeal-cli-${{get_version.outputs.version}}.exe
      #       dist/zeal_cli-1.2.0.tar.gz
      #       zeal_cli-1.2.0-py3-none-any.whl
